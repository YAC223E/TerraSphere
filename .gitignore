# Geographic raw cache + oversized global 10m (refetch via npm run fetch:data; runtime uses africa-10m subset)
raw-data/
public/data/ne_10m_admin_0_countries.geojson

# Headless QA screenshots (regenerate via npm run qa)
qa/screenshots/

# Env / secrets — never commit tokens
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
